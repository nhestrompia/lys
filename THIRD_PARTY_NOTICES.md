# Third-Party Notices

The current package has no fetched Swift package dependencies and contains no bundled agent adapter, WebDriverAgent, or AXe binary.

Validated downloads must preserve their upstream notices. WebDriverAgent is maintained by Appium and distributed under the BSD 3-Clause license. The app downloads its pinned archive only on explicit setup and verifies the promoted SHA-256 before use. Apple SDKs, Xcode, Simulator, XCTest, and system frameworks are Apple-provided prerequisites and are not redistributed by this repository.

AXe is maintained by Cameron Cooke and distributed under the MIT License. Source builds may use an externally installed AXe 1.8.0 helper for the continuous CoreSimulator framebuffer and HID broker. AXe builds on Meta's FBSimulatorControl/private Xcode frameworks and is therefore compatibility-gated per Xcode release; no AXe or FBSimulatorControl binary is currently redistributed by this repository.

The Codex, Claude, OpenCode, and pi SVG marks under `Sources/IOSDevApp/Resources/AgentIcons` come from the official Agent Client Protocol Registry and remain the property of their respective projects or owners. They are used only to identify compatible integrations.

The generated UI concept images under `.impeccable/mocks` are design-development artifacts, not runtime dependencies.
